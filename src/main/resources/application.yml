eureka:
  client:
    fetch-registry: false
    healthcheck:
      enabled: true
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761
server:
  port: 8761
spring:
  application:
    name: eureka-server
